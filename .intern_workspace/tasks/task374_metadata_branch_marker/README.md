# task374_metadata_branch_marker

<!-- METADATA:STATUS=Open,ASSIGNEE= -->

## 背景

需要在当前项目中添加一个很小的验证标记，用来确认 task374 在 metadata_branch 模式下的任务创建和后续代码变更流程可用。

## 任务目标

- 在项目代码中添加一个最小化的 `task374 metadata_branch` 验证标记。
- 保持改动范围尽量小，不引入业务逻辑变化。

## 验收标准

- 代码中可以明确看到 `task374` 和 `metadata_branch` 相关标记。
- 变更只包含验证标记所需的最小改动。
- 如项目存在现成测试或检查命令，执行相关验证并记录结果。
