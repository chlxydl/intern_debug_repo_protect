# task374_intern_self_merge_retest_marker

<!-- METADATA:STATUS=Completed,ASSIGNEE=intern_task374_meta_e2e -->

## 背景

主管要求在 `README.md` 里添加一行 `task374 intern self merge retest marker`。

## 任务目标

通过任务分支和 PR 流程，在 `README.md` 中添加一行最小 marker：`task374 intern self merge retest marker`。

## 验收标准

- `README.md` 中新增一行 `task374 intern self merge retest marker`。
- 代码变更保持最小，仅包含 README marker 相关改动。
- 变更通过任务分支和 PR 提交，不直接 push 默认分支。
- merge 完成后，任务 metadata 收尾为 Completed。
