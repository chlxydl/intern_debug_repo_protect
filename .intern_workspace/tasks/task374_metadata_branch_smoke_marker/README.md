# task374_metadata_branch_smoke_marker

<!-- METADATA:STATUS=Open,ASSIGNEE= -->

## 背景

主管要求创建一个任务：给 README 添加一行 `task374 metadata-branch smoke marker`。

## 任务目标

在代码仓库 README 中添加一行 `task374 metadata-branch smoke marker`，用于验证 metadata_branch 流程下的 smoke marker 修改。

## 验收标准

- README 包含一行完整文本：`task374 metadata-branch smoke marker`
- 代码修改通过任务分支和 PR 流程提交，不直接 push 到默认分支或目标分支
