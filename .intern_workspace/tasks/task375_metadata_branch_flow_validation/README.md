# task375_metadata_branch_flow_validation

<!-- METADATA:STATUS=Completed,ASSIGNEE=intern_fresh_meta -->

## 背景

主管要求创建一个任务，在代码仓库的 `README.md` 中增加一行：

```text
metadata_branch flow validated by intern_fresh_meta
```

## 任务目标

在 `README.md` 增加指定文本行，并通过任务分支与 PR 流程提交代码修改。

## 验收标准

- `README.md` 包含一行完整文本：`metadata_branch flow validated by intern_fresh_meta`
- 代码修改不直接 push 到默认分支或目标分支。
- 代码修改通过任务分支提交，并按仓库流程创建 PR。
