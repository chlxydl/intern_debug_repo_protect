# task374_metadata_branch_validation

<!-- METADATA:STATUS=InProgress,ASSIGNEE=intern_t374_meta -->

## 背景

主管要求创建一个任务：在 README.md 中添加一行 `task374 metadata_branch validation`。

## 任务目标

在代码仓库的 `README.md` 中添加一行：

```text
task374 metadata_branch validation
```

## 验收标准

- `README.md` 包含独立一行 `task374 metadata_branch validation`。
- 代码修改通过任务分支和 PR 流程提交，不直接 push 代码默认分支或目标分支。
