# task374_metadata_branch_marker

<!-- METADATA:STATUS=Open,ASSIGNEE= -->

## 背景

当前 workspace 运行在 `metadata_branch` 模式，需要一个很小的 task374 验证改动，用于确认任务元数据创建与分支流程可正常工作。

## 任务目标

- 在当前项目中添加一个很小的 `task374 metadata_branch` 验证标记。
- 标记应尽量局部、清晰，不引入无关功能或大范围重构。

## 验收标准

- 项目中存在可识别的 `task374 metadata_branch` 验证标记。
- 改动范围很小，且不影响现有功能。
- 如项目有现成测试或检查命令，执行相关验证并记录结果。
